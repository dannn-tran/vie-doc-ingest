[project]
name = "vie-doc-ingest-cli"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "typer",
    "gc-vision-adapter",
]

[tool.uv.sources]
gc-vision-adapter = { workspace = true }

[build-system]
requires = ["uv_build>=0.8.11,<0.9.0"]
build-backend = "uv_build"